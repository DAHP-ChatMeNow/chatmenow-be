const CONVERSATION_TYPES = {
  PRIVATE: "private",
  GROUP: "group",
};

const CONVERSATION_REQUEST_STATUS = {
  ACCEPTED: "accepted",
  PENDING: "pending",
};

const MEMBER_ROLES = {
  ADMIN: "admin",
  MEMBER: "member",
};

module.exports = {
  CONVERSATION_TYPES,
  CONVERSATION_REQUEST_STATUS,
  MEMBER_ROLES,
};
